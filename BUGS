o) The signal mask stored for the signal handler does not work yet.

o) More verification of signal handler arguments should be done.  The machine
   context structure may be of particular interest.

